{
  "name": "movare-gerador",
  "version": "1.0.0",
  "description": "MOVARE · Gerador de Conteúdo com IA (Groq)",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "groq-sdk": "^0.5.0"
  }
}
